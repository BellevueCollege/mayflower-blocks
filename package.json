{
  "name": "mayflower-blocks",
  "version": "3.2.2",
  "private": true,
  "workspaces": [
    "blocks/*"
  ],
	"scripts": {
		"build" : "npx lerna run build",
		"start" : "npx lerna run start"
	},
  "devDependencies": {
    "lerna": "^6.0.3",
    "nx": "^15.2.1"
  }
}
